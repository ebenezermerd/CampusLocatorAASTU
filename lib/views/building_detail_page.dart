import 'package:flutter/material.dart';

class BuildingDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Building Details'),
      ),
      body: Center(
        child: Text('Building Detail Page'),
      ),
    );
  }
}